# DECISIONS

Append-only. One line per decision, dated, with its reason. Never edit or delete a
past entry; if a decision is reversed, append the reversal with its own date and
reason. The reason matters as much as the decision: it is what tells a later
session whether the ground has shifted.

## Seeded from PLAN.md section 1 (approved plan, 2026-08-26)

- 2026-08-26 — Display name "The Pastor Bible". Repo slug "pastor-bible" on Jared's personal GitHub. No org: not a business, never sold.
- 2026-08-26 — Zero cost, forever. No fees of any kind. Every dependency is free and open source.
- 2026-08-26 — Repo license Apache-2.0. Permissive, OSI-approved, explicit patent grant, formal NOTICE file for source attribution.
- 2026-08-26 — RAG over the actual text, not fine-tuning. Fine-tuning yields confident fake references; retrieval yields checkable ones.
- 2026-08-26 — Text: World English Bible, Classic edition (US spelling, "Yahweh"). Public domain worldwide; no Crown-copyright quirk. Trademark condition: we ship an unmodified faithful copy, so we may call it the World English Bible.
- 2026-08-26 — Canon: 66 books always on. One toggle, off by default, adds the WEB Deuterocanon/Apocrypha. Every Deuterocanon citation is labeled in output. 1 Enoch, Kybalion, and all non-biblical texts are OUT of this app (section 15).
- 2026-08-26 — Index corpora: Treasury of Scripture Knowledge (TSK) cross-refs and Nave's Topical Bible. Public domain. No commentary of any kind: commentary is one tradition's interpretation.
- 2026-08-26 — Sources are structured text only (USFM/JSON/plain text with verse delimiters). No PDFs: extraction breaks verse boundaries.
- 2026-08-26 — Internet is used exactly twice: downloading the installer from GitHub, and downloading the model file on first run. After that the app makes no outbound connection, and a test proves it.
- 2026-08-26 — Model server: llama.cpp (MIT), bundled as a Tauri sidecar. Not Ollama, which installs a separate always-on service with its own tray icon and updater.
- 2026-08-26 — Models (chat, embedding, reranker): Apache-2.0 or MIT only. No Llama community license, no Gemma terms, no acceptable-use policy flowing to users.
- 2026-08-26 — Shell: Tauri 2 (MIT/Apache). Real installer, icon, owns the model server's lifecycle, cross-platform by design.
- 2026-08-26 — Targets: Windows and Linux. macOS dropped (notarization requires a paid Apple account). Mac users may build from source.
- 2026-08-26 — Signing: ship unsigned first (SignPath requires an existing release). Apply to SignPath Foundation after the first public release. README states up front that Windows will show a SmartScreen warning and shows exactly how to proceed.
- 2026-08-26 — Upgrades are manual re-downloads, and the installer upgrades in place: it detects any previous version and replaces it. Never two versions on one machine. User data (history, settings, downloaded models) lives outside the install directory and survives every upgrade; only program and index are replaced.
- 2026-08-26 — Model size: default = one size up from the smallest model that passes evaluation. Installer auto-selects the smaller passing model on machines below the RAM threshold.
- 2026-08-26 — Zero fabricated references, enforced mechanically (5.6), not by prompt alone.
- 2026-08-26 — Question history stored locally; searchable, deletable, exportable. Never leaves the machine.
- 2026-08-26 — Prebuilt index ships inside the installer. The user's machine never parses sources or builds embeddings.
- 2026-08-26 — README opens with a study/educational disclaimer and a crisis note covering harm to self or others. The crisis note also appears in-app above (never instead of) an answer when crisis language is detected.
- 2026-08-26 — Credit: Jared and Claude, both, in README and About screen.
- 2026-08-26 — Workflow: this plan lives in project instructions. Claude Code executes one phase per session (section 13). Jared directs.

## P0 session, 2026-08-26

- 2026-08-26 — Repo is private until v1.0.0 is released, then made public. Reason: nothing to show or support before a working release.
- 2026-08-26 — End-user install has zero manual prerequisites: the Windows installer bootstraps the WebView2 runtime if absent; first run checks RAM and disk (plan 7.1) and refuses cleanly if below floor; the user never installs a runtime, toolchain, or dependency by hand. Reason: non-technical users; plan section 0.
- 2026-08-26 — Windows installer runs per-user (Tauri NSIS installMode currentUser): no admin/UAC prompt, installs under the user's AppData. Trade-off: one user account per machine. Confirmed or reversed in P6. Reason: fewest clicks; works on machines where the user is not an administrator.
- 2026-08-26 — Product identifier io.github.haomuch1.pastorbible. Reason: fixed product id required for in-place upgrades (plan 7.5); sets app data path; permanent after v1.0.0.
- 2026-08-26 — Copyright holder is Jared; Claude credited as co-author in README, About, and NOTICE. Reason: Claude cannot hold copyright.
- 2026-08-26 — CODE_OF_CONDUCT.md is Contributor Covenant 2.1, licensed CC BY 4.0, shipped unmodified. Reason: Jared's call when told 3.0 is now current under CC BY-SA 4.0; 2.1 avoids a share-alike term and is the version most contributors expect.
- 2026-08-26 — The Contributor Covenant's "[INSERT CONTACT METHOD]" placeholder is left in place. Reason: shipping it unmodified was the instruction; a real reporting address must be filled in before the repo goes public at v1.0.0.
- 2026-08-26 — data/crisis_terms.txt is not created in P0, though plan section 12 lists it. Reason: an empty crisis-term list matches nothing, and plan 5.8 holds that under-triggering is unacceptable; the file is created with real content in P4, where the crisis matcher is built.
- 2026-08-26 — Frontend template is React + TypeScript + Vite via create-tauri-app; package manager npm. Reason: plan 3.4 and section 16 name React + TypeScript; npm ships with Node and adds no toolchain.
- 2026-08-26 — Git identity for this repo is set locally to Jared with his address, not globally. Reason: the repo records Jared as author; no machine-wide config is changed.
- 2026-08-26 — .gitattributes normalizes all text to LF in the repository and on checkout ("* text=auto eol=lf"). Reason: git's autocrlf was on, which would have checked docs/PLAN.md out with CRLF and broken its byte-identity with the approved plan; it also keeps Windows and Linux checkouts identical for the cross-platform build.
- 2026-08-26 — tauri-plugin-opener, serde and serde_json were removed from the scaffold. Reason: P0 requires only a window; the opener plugin grants a capability to launch external programs, which an offline app should not carry until something needs it.
- 2026-08-26 — Commit author email is the GitHub noreply address 293447797+haomuch1@users.noreply.github.com, set locally for this repo only. Reason: GitHub rejected the first push with GH007 rather than publish Jared's private address; noreply keeps that protection on and keeps his real address out of a repo that becomes public at v1.0.0.
- 2026-08-26 — Application icons remain the stock Tauri logo for now. Reason: icon design is an aesthetic choice and belongs to Jared; must be replaced before packaging in P6.

## P1 session, 2026-08-26

- 2026-08-26 — CODE_OF_CONDUCT.md contact method is "by opening an issue on the pastor-bible GitHub repository"; the preceding word "at" was dropped so the sentence is grammatical. Reason: the instructed replacement inserted verbatim produced "at by opening an issue"; the CoC is public-facing and must read correctly. Reverses the P0 entry that left the placeholder in place.
- 2026-08-26 — NOTICE.md now records the CoC as modified, with both the new and the upstream checksum. Reason: CC BY 4.0 requires modifications to be indicated, so editing the file obliged the notice to change with it.
- 2026-08-26 — 9.4 reworded to future tense until P8; revert at P8. Reason: plan text must be true at every commit.
- 2026-08-26 — docs/PLAN.md is no longer byte-identical to the plan as approved; its sha256 is now c2431f31134cd192d254ec137801d3d5c650e52aacf059fadc375f75b22cde48, was f07a7354683ad38a4be0219651a5b3fca23ed6ad534dbc1fed60fcff7ad57239. Reason: the 9.4 rewording above was applied to the plan as well so plan and README cannot drift; the plan is now a living document and future sessions must not treat its hash as fixed.
- 2026-08-26 — crisis_terms.txt exists as a visibly empty placeholder; P4 owns content and enforcement. Reason: reverses the P0 decision to omit the file; the file now states in its own text that it matches nothing and that P4 tests must fail while it has no terms, which removes the risk that made omitting it the safer choice in P0.
- 2026-08-26 — Text source is eBible.org "eng-web", the World English Bible Classic with the full ecumenical book set, USFM. Reason: it is the only eBible WEB edition that is both American-dialect and carries all 15 deuterocanonical books, and its copyright file states public domain outright.
- 2026-08-26 — TSK and Nave's come from the CrossWire SWORD modules (TSK 1.4, Nave 3.0), whose own config files declare DistributionLicense=Public Domain. Reason: they are the canonical electronic editions of the original works rather than re-curated derivatives, and the alternatives found were either a deliberately edited derivative, PDF-extracted, or unlicensed.
- 2026-08-26 — A candidate Nave's dataset derived from a PDF was rejected. Reason: the locked decision forbids PDF sources because extraction breaks verse boundaries, and consuming a PDF-derived dataset inherits exactly that risk.
- 2026-08-26 — The SWORD zCom and zLD binary formats are decoded by our own readers in pipeline/sword.py, with the layout asserted at read time. Reason: no library was needed once the layout was established, and a reader that raises on a mismatch is safer than one that returns plausible wrong data.
- 2026-08-26 — Schema deviates from PLAN 3.2: verses.verse_end for bridges; tsk_unresolved and nave_unresolved quarantine tables; books.usfm_code; order renamed book_order (reserved word). Reason: bridges and unresolved references must be visible rather than silently lost, per the zero-fabricated-references stance.
- 2026-08-26 — Verses whose USFM \v marker carries no text are not given a row. There are 29, each one a verse the WEB omits with a footnote saying so. Their count and full list are recorded in meta. Reason: an empty verse row cannot be displayed or cited, and inventing one would be a fabricated reference.
- 2026-08-26 — Deuterocanonical books are recorded as testament=OT with canon=deutero. Reason: the schema needs a testament value, all 15 sit in the OT section, and the protestant/deutero distinction is carried by the canon column.
- 2026-08-26 — Nave references are resolved from the TEI osisRef attributes rather than from the displayed reference text. Reason: osisRef is already canonical and unambiguous; only 31 of its references failed to resolve.
- 2026-08-26 — Every reference from either corpus is resolved against the verses actually parsed from the WEB, and anything that does not land on a real row is quarantined with a reason rather than dropped. Reason: a cross-reference the app cannot display is one it must not claim.
- 2026-08-26 — The build is deterministic and its build date defaults to 1970-01-01 so repeat builds are byte-identical. Reason: plan 4.3 requires same sources plus same model to yield the same checksum; a real date is stamped by the release job that publishes an index.
- 2026-08-26 — .gitattributes marks *.zip as binary explicitly. Reason: the vendored source archives have their checksums recorded in NOTICE.md, and no line-ending normalization may ever touch them.

## P2-prep session, 2026-08-26

- 2026-08-26 — Footnotes and cross-references are omitted from verse text, permanently for v1. Reason: eBible.org permits extracts, listing "quote" among the permitted uses; the WEB trademark condition applies "if you CHANGE the actual text of the World English Bible in any way", and we never change the text, only omit the apparatus around it.
- 2026-08-26 — NOTICE.md WEB entry states plainly that verse text is reproduced unmodified and that translator footnotes and cross-references are omitted. Reason: the one legal obligation on our main source deserves an explicit, checkable statement rather than an inference.
- 2026-08-26 — Eval set is 20 graded questions with MUST and SHOULD gold lists, plus 20 ungraded smoke questions with no gold lists, run in P3 for Jared's eyeball review only. Reason: keeps Jared's approval burden proportionate; the smoke set preserves topic coverage.
- 2026-08-26 — Gold list structure: MUST = 5-8 passages a pastor would say the answer cannot omit, and recall@25 in P2 is measured against MUST only; Jared approves every MUST list. SHOULD = further relevant passages, Claude's draft, non-gating, labeled unreviewed. Reason: only the gating part needs approval.
- 2026-08-26 — A gold passage is a verse range within one chapter, never a whole chapter or book. Reason: a chapter-wide gold entry cannot distinguish good retrieval from lucky retrieval.
- 2026-08-26 — Known limitation, measured in P2: TSK and Nave's index the 66 protestant books only, so Deuterocanon passages are reachable by vector and keyword search alone. Questions g19 and g20 exist to measure this. No fix is built until P2 numbers show one is needed.
- 2026-08-26 — Pericopes remain WEB paragraphs; no headings are added. P2 reports paragraph length distribution and recall before any change is considered. Reason: no public-domain heading set exists, and generated headings would inject interpretation.
- 2026-08-26 — A MUST candidate must carry at least two of the three origins (Nave's, FTS, TSK); a single-origin passage drops to SHOULD rather than being discarded. Reason: a passage the answer cannot omit should be corroborated by more than one source, and a passage that is right but uncorroborated is still recoverable from SHOULD.
- 2026-08-26 — Nave's topic weight falls as the topic grows (3.0 * 200/(200+n) per verse). Reason: membership of a 40-verse topic is strong evidence about a verse and membership of an 1855-verse topic is nearly none; unweighted, the large topics filled MUST lists with passages that shared a theme but did not answer the question.
- 2026-08-26 — Candidate ranges are ranked by score density, total score divided by the square root of the range length. Reason: without it a long range wins on volume alone.
- 2026-08-26 — Keywords use WEB vocabulary, notably "fear of Yahweh" rather than "fear of the Lord". Reason: the WEB Classic renders the divine name as Yahweh, so the conventional phrasing matches nothing.

## P2 session, 2026-08-26

- 2026-08-26 — Gold lists are approved as drafted. They are index-derived (Nave's, TSK, FTS) and unreviewed by a pastor; docs/EVAL.md and data/eval/questions.json say so in those words. Jared is not a pastor and declines to hand-judge; a future contributor may revise. Reason: honest standard for a scripture-only tool; no better ground truth exists at zero cost.
- 2026-08-26 — The six Deuterocanon passages set aside in the P2-prep handoff are added to the g19 and g20 MUST lists, tagged deutero, approved the same way. Reason: without them the canon test measures nothing.
- 2026-08-26 — Because the gold lists share sources with the topic, cross-reference and keyword retrieval paths, full-pipeline recall against them is near-circular; it is reported but not treated as evidence. The primary retrieval metric is ablation: vector-only recall@25 against MUST. Reason: measures what embeddings add, which is the only open question.
- 2026-08-26 — Vector store is plain float32 BLOBs in SQLite tables, searched brute force by cosine. sqlite-vec is not used. Reason: about 70,000 small vectors search in milliseconds, and no native extension has to be bundled into the installer. PLAN 3.2 updated and the sqlite-vec VERIFY item in section 16 struck.
- 2026-08-26 — The embedding and reranker models must run under llama.cpp at runtime, and index-time embeddings are produced through llama.cpp too, so index and query vectors come from identical code. Reason: eliminates train/serve mismatch.
- 2026-08-26 — PLAN 7.2 amended: the passage panel shows the full retrieved and expanded set grouped by book, cited passages marked and the rest collapsed one click away, with matched Nave's topics shown by name and their full verse lists. Reason: the reader chooses what to read; scripture-first.
- 2026-08-26 — PLAN 5.6 and 7.2 amended: two answer modes, the default themed synopsis over the top ~25 passages and, on demand, a themed summary of the entire retrieved set built in batches and merged, with the citation verifier applied at every stage including the merge. Never the default; P3 measures latency and quality first. Reason: full-set reading is impractical, and the summary must still be verifiable.
- 2026-08-26 — The MUST size rule is amended for g19 and g20 only, which now carry 14 entries: the 8 drafted protestant passages plus 6 deuterocanonical ones. Reason: deuterocanonical passages can never clear the two-origin bar because neither study corpus indexes those books, so without adding them by hand the canon toggle has nothing to be measured against.
- 2026-08-26 — Deuterocanonical MUST entries are exempt from the two-origin corroboration rule and carry origins ["fts"] alone. Reason: keyword search is the only origin available to them; the exemption is asserted in the tests so it cannot silently widen.
- 2026-08-26 — Gold ranges exclude verses the WEB omits. Sirach 19:20-22 stores verses 20 and 22 only, because 19:21 has no text. Reason: a gold list must not claim a verse the index cannot produce.
- 2026-08-26 — Documents longer than the embedding model context are truncated to fit rather than skipped, and the truncation is applied to the text after the model prefix. Reason: llama-server refuses an over-long input outright, and a refused document would leave a silent hole in the index; a truncated vector is worse than a full one but far better than none.
- 2026-08-26 — Some Nave's subtopic headings are long prose rather than headings, the longest running to 1,243 tokens. They are embedded truncated. Reason: P1 derived subtopic labels from the entry text, and Nave's writes some subtopics as full sentences; re-deriving them is a P1 change and is not worth reopening for a handful of rows.
- 2026-08-26 — llama-server is started with a physical batch of at least 2048 regardless of the model context. Reason: the batch must hold the longest single input, which is a different constraint from the context window, and a smaller batch makes the server reject long documents outright.
- 2026-08-26 — RECOMMENDED, confirmed in P3: embedding model nomic-embed-text-v1.5 (Apache-2.0, 137M, dim 768). Reason: it leads on every configuration measured and its 2048-token context leaves only 3 pericopes needing a split against bge-small's 106, while costing 251 MB resident against qwen3's 946 MB; the gap over both is inside the confidence interval, so the tie is broken on size and context, not on a claimed win.
- 2026-08-26 — RECOMMENDED, confirmed in P3: no reranker. Reason: bge-reranker-v2-m3 lowered recall@25 for all three embedding models, from 0.629 to 0.529 for the recommended one, while adding about 10 seconds per query and 2 GB of resident memory against 3 to 24 milliseconds for retrieval itself. It is the only reranker that met the licence and GGUF conditions, so "no reranker" is the finding rather than a preference.
- 2026-08-26 — RECOMMENDED, confirmed in P3: configuration F, vector verses plus vector pericopes plus FTS, fused by reciprocal rank fusion, then Nave's topic expansion and one TSK hop. Reason: highest recall of the configurations that ship, and the expansions are what fill the passage panel that PLAN 7.2 now specifies.
- 2026-08-26 — qwen3-embedding-0.6b is rejected as the default. Reason: its advantage over nomic is +0.034 recall@25 with a 95 per cent bootstrap interval of [-0.045, +0.112], which includes zero, against 3.8 times the resident memory, a 2.7-hour index build rather than 23 minutes, and 275 MB rather than 206 MB added to the installer.
- 2026-08-26 — bge-small-en-v1.5 is retained as the fallback embedding model if P3's hardware floor proves tight. Reason: 75 MB resident and 102 MB of vectors, at a recall cost that this eval set cannot prove is real.
- 2026-08-26 — Canon mode is confirmed not to be a filter over a fixed ranking: enabling the Deuterocanon displaces 5 to 15 of the 25 protestant passages, for every model and configuration tested. Reason: measured in P2; no code may assume the 66-book result is a subset of the both-canon result, and P5 must decide whether a reader turning the setting on should be told they will see different protestant passages.

## P3 session, 2026-08-26

- 2026-08-26 — The graded set for P3 is 10 questions: g01, g03, g04, g05, g08, g11, g12, g13, g14, g18. The other ten graded questions keep their gold lists but move to the smoke pool, which becomes 30 questions, of which P3 runs 10 and 20 are reserved for post-v1 testing. Reason: Jared's direction; exhaustive testing happens after v1.0.0, not now.
- 2026-08-26 — Canon priority: every gate, selection, smoke run, summarize-all test and hardware-floor number is measured in canon 66 only. Deuterocanon results are reported separately and never gate. Reason: the 66 books are the product.
- 2026-08-26 — Execution is strictly sequential: one chat model loaded at a time, one question at a time, llama-server run with a single slot and no parallel requests, and each question's metrics row written before the next begins. Reason: one thing at a time, and a partial session still leaves usable committed evidence.
- 2026-08-26 — Machine safety: llama.cpp uses its default thread count, no model is loaded whose GGUF size plus 2 GB headroom exceeds free RAM, never two model processes at once, and both server and harness run below normal priority. Reason: the machine must stay usable and must never swap.
- 2026-08-26 — Additive canon retrieval: in both-canon mode the harness runs retrieval under canon 66 exactly as in 66 mode, then a Deuterocanon-only retrieval, and appends its top slice (default 8) labeled. The 66 result is therefore always a subset of the both result. Reason: P2 measured up to 15 of 25 protestant passages displaced when the Deuterocanon was simply unfiltered.
- 2026-08-26 — The embedding model (nomic-embed-text-v1.5, sha256 in EVAL.md) is bundled in the installer rather than downloaded on first run. Reason: it is essential to every query, it is 262 MB, and it is one fewer download that can fail. P6 confirms.
- 2026-08-26 — No reranker in v1. Reason: P2 measured a recall@25 loss from 0.629 to 0.529 with 2 GB and about 10 seconds of cost.
- 2026-08-26 — No answer-quality rating rubric and no Jared rating pass. Smoke answers are written to docs/SMOKE.md for optional reading. Reason: the measured gates are fabricated-reference count and citation precision; a 1-to-5 rubric on 10 questions would add a number without adding evidence.
- 2026-08-26 — Prompts live in data/prompts/ as plain text with a version line: rewrite, synopsis, retry, summarize_batch, summarize_merge, all version 1. Reason: a prompt is part of the product's behaviour, so it is versioned and diffable rather than buried in code; every change gets its own DECISIONS line.
- 2026-08-26 — P3 gate thresholds, fixed before the 4B and 8B results were seen. (1) First-pass verifier violation rate at or below 0.30. Reason: a violation costs a full second generation, so a third of questions taking roughly twice as long is the most latency the retry path may add. (2) Fallback rate below 0.10, per the session brief. (3) Structure compliance 100 per cent, scored as the brief defines it: theme headings present, every theme cites at least one sent token, no theme cites an unsent token. (4) Median end-to-end latency at or below 180 seconds on this CPU, with no question above 300. Reason: three minutes is already a long wait for a desktop answer, and the plan's hardware floor has to be honest about it.
- 2026-08-26 — Structure compliance is scored from the stored outputs by pipeline/report_p3.py, using one definition for every model, because the harness's own check was tightened mid-session after the first model had already run. Reason: a metric must mean the same thing for every candidate it compares.
- 2026-08-26 — The retry loop is citation-only by design (docs/VERIFIER.md). A structurally poor but citation-clean answer is not retried. Reason: the verifier's contract is references, and widening it to prose shape would make the Rust port in P4 a style judge as well as a citation checker.
- 2026-08-26 — SELECTED: default chat model Qwen3-8B (Q4_K_M, Apache-2.0); fallback Qwen3-1.7B (Q8_0, Apache-2.0). Qwen3-4B fails two gates and is rejected. Reason: applying plan 6.4 to the gate results, the smallest model passing every gate is 1.7B and so becomes the fallback, and the next size that passes is 8B, because 4B fails structure compliance at 0.70 and exceeds the 300-second ceiling at 377.9 seconds on one question.
- 2026-08-26 — Qwen3-4B is rejected despite sitting between the two chosen sizes. Reason: 30 per cent first-pass violations, structure compliance 0.70 with three answers carrying no theme heading at all, citation coverage 0.333 against 0.79 and 0.82 for its neighbours, and a 377.9-second worst case. Size did not order quality here.
- 2026-08-26 — All three chat candidates come from the Qwen3 family. Reason: it is the only current-generation family offering Apache-2.0 weights with first-party GGUF builds at all three size classes, and holding the family constant makes size the variable that plan 6.4 actually selects on. Phi-4-mini (MIT) is the alternative family if P4 or later wants a cross-check.
- 2026-08-26 — Qwen3 reasoning is disabled with its own /no_think switch and any <think> block is stripped before verification. Reason: the reader never sees reasoning tokens, so they must not be part of what the verifier checks, and leaving them on multiplies latency for output nobody reads.
- 2026-08-26 — FINDING, not yet acted on: model query rewriting lowers retrieval recall for every candidate, by 0.162, 0.262 and 0.250 recall@25 against the hand-written keyword lists. This contradicts the expectation behind PLAN 5.2. Reason to flag rather than change: 5.2 is an approved plan decision and 10 questions is a small sample; P4 should measure it again before the rewrite step is kept, dropped, or made additive to the raw question.
- 2026-08-26 — Hardware floor, measured on an AMD Ryzen 7 5800X, CPU only: default configuration 16 GB RAM and about 5.3 GB disk; fallback configuration 8 GB RAM and about 2.3 GB disk. Reason: plan 6.5 requires measured numbers; the RAM figures are a judgement drawn from a controlled single-answer measurement (8998 MB resident, 4615 MB private for the 8B) and an observed ten-question worst case (15068 MB), and README says so. P7 re-tests on a clean machine.
- 2026-08-26 — FINDING: the summarize-all path of PLAN 5.6 took 33 minutes on the largest retrieved set (548 passages, 6 batches), peaked at 16.9 GB, and dropped 130 of the 253 passages its batches had cited when merging. The citation guarantee held at every stage. Reason to record rather than build: P4 must either use a much smaller model for the batch stage, or a merge that cannot drop citations, or re-scope the feature honestly.
- 2026-08-26 — The additive canon top-N cut sends the canon-66 top slice plus the Deuterocanon slice, rather than a longer prefix of the ranked full set. Reason: the appended slice sits at the bottom of the full set by construction, so a prefix would return more protestant passages and never reach it. Two bugs of exactly this shape were found and fixed in P3, one in the retriever and one in the run harness.

## P4 session, 2026-08-26

- 2026-08-26 — Summarize-all is removed from v1. PLAN 5.6 and 7.2 are rewritten: the full passage set is grouped under the matched Nave's topic headings, topics ordered by match strength, passages within a topic in canonical order, and passages matching no topic under "Other passages"; expandable, no model call. Reason: P3 measured 33 minutes, 16.9 GB and 130 of 253 cited passages lost at the merge; a grouping that cannot fabricate and costs nothing serves the same need. Post-v1 candidate on GPU hardware.
- 2026-08-26 — Query rewrite is decided by measurement in this session's STEP 6, between the raw question, the model rewrite, and the two fused. Reason: P3 measured the rewrite below hand-written keywords, but hand keywords do not exist at runtime; the real alternative is the raw question.
- 2026-08-26 — Waiting experience (built in P5): retrieved passages are displayed as soon as retrieval returns; the synopsis appears only after the verifier passes; progress is a stage indicator and a token counter, never streamed text. Reason: no unverified reference is ever visible, even briefly, and the reader can start in the text while generation runs.
- 2026-08-26 — A Vulkan build of llama-server is measured in this session as a candidate second sidecar. Bundling and runtime selection are decided in P6. Reason: CPU-only latency is 2 to 4 minutes on a fast desktop.
- 2026-08-26 — llama-server is launched with a context size derived from the measured prompt length plus the output budget plus a margin, not the model's maximum. Reason: P3's 15 GB peak for the 8B is likely KV-cache from an oversized context; re-measured this session.
- 2026-08-26 — README's hardware section states in one sentence that the fallback model produces list-style answers. Reason: it passes every gate, reads worse, and users should know before choosing it.
- 2026-08-26 — A cross-family model check (Phi-4-mini) is deferred to post-v1. Reason: Jared's rule.
- 2026-08-26 — The citation verifier's Rule B could not detect any multi-word book name. Fixed in pipeline/verifier.py and in the Rust port together: the reference pattern is now built word by word from the written form instead of from the space-stripped normalised key, and a short alias list adds the common English names the WEB's titles and the TSK table do not spell out. Reason: measured against 83 realistic names, 14 were invisible, including "Song of Solomon", "Acts of the Apostles" and eleven deuterocanonical names, which is a hole in the locked zero-fabricated-references decision. Jared's call, given the alternative of porting the gap for parity's sake.
- 2026-08-26 — The verifier contract grows from 25 test vectors to 35: six multi-word and deuterocanonical references that must be flagged, and four of the same names in ordinary prose without a chapter number that must not be. Reason: a detection fix that trips on "the song of songs was sung" would cost the reader a needless retry, so both directions are pinned by the contract.
- 2026-08-26 — The fix is proven to change no P3 result: 41 first-pass verdicts, 14 violation records compared field by field including spans, and 41 final answers, all identical before and after. Reason: P3's selection numbers must remain valid evidence, and a verifier change is exactly the kind of thing that would quietly invalidate them.
- 2026-08-26 — The Rust retriever accumulates its cosine dot products in f64 although both operands are f32. Reason: a sequential f32 sum ranked Pro 2:5 above Psa 19:9 on question g08 where the harness has them the other way; their true cosines differ by 1.8e-7, numpy's BLAS lands within 1e-9 of exact and a sequential f32 sum drifts 2.4e-7, so f64 is both the more accurate answer and the one that reproduces the harness.
- 2026-08-26 — The Rust score map is insertion-ordered rather than a hash map. Reason: Python dicts preserve insertion order, and both the TSK anchor list and the TSK keep-list are stable sorts over that order, so a hash map would have produced a different expansion and the parity fixtures would not reproduce.
- 2026-08-26 — Parity fixtures are committed JSON under src-tauri/core/tests/fixtures, 3.5 MB, holding the query vector, the full candidate set, the passages, the sent cut and the matched topics for 14 retrieval cases, plus the 35 verifier vectors and all 82 P3 model outputs. Reason: a parity claim tested against a live Python process is not reproducible from a checkout; against committed artefacts it is.
- 2026-08-26 — The Rust core is a separate crate, src-tauri/core, that the Tauri shell depends on. Reason: retrieval, the verifier and the CLI harness carry no GUI dependency, and the tests that matter should not have to build a webview to run.
- 2026-08-26 — Query mode default is `raw`: the reader's question embedded on its own, with its content words as the FTS terms. `--query rewrite` and `--query fused` remain available. Reason: measured on the ten graded questions, recall@25 against MUST is raw 0.3625, rewrite 0.3500, fused 0.4000; a paired bootstrap puts fused minus raw at +0.0375 with a 95 per cent interval of [-0.0125, +0.0875], which includes zero, so by the same standard this project applied to the embedding models the three are not separable, and the tie is broken on cost: fused needs an extra chat-model load and a generation per question, raw needs neither. PLAN 5.2's rewrite step is therefore off by default and one flag away.
- 2026-08-26 — In raw mode the FTS terms are the question's content words, not the question itself. Reason: a term containing a space is quoted as an FTS phrase, and no verse contains the phrase "what does the bible say about anxiety and worry", so the whole keyword path would have returned nothing. The stopword list lives in the Rust source and the Python measurement reads it from there, so the two cannot drift.
- 2026-08-26 — Hand-written keyword lists remain the best retrieval input measured, at 0.4875 recall@25 against raw's 0.3625, and are not available at run time. Reason: recorded so a later session does not rediscover it as news; closing that gap is a retrieval problem, not a prompt problem.
- 2026-08-26 — TopicOut and TopicGroup carry heading_display beside heading. Reason: Nave's writes some subtopics as full paragraphs, so the raw heading is unusable as a group label; the source text stays available and P5 chooses.
- 2026-08-26 — FINDING: P3's 15,068 MB peak is not an oversized context window. Measured prompt lengths over the ten graded questions are 2,709 to 5,819 tokens; with the 900-token output budget and a 25 per cent margin the derived context is 8,398, which is larger than the 8,192 in use. The difference is server lifetime: P3 kept one llama-server alive across ten questions and the Rust CLI starts a fresh one per question, which peaks at 9,001 MB every time. Reason to record: P5 will keep a server alive between questions for speed and must re-measure before README's 16 GB floor is trusted.
- 2026-08-26 — Context stays at 8,192 rather than moving to the derived 8,398. Reason: the derived figure is above the current one, so there is nothing to trim, and 8,192 is a power of two that the worst measured question fills to 82 per cent; raising it would raise the KV cache for no measured benefit. Revisit if a question ever exceeds the budget.
- 2026-08-26 — The concurrent-sidecar flag is real rather than cosmetic: with --allow-both-servers the embedding server stays up and the chat server starts beside it, the free-RAM check runs for the second model with the first already resident, and the answer records which path actually ran rather than which was asked for. Reason: a flag that only changes a label is worse than no flag, because the record it writes is false.
- 2026-08-26 — PLAN 5.7's visible Deuterocanon tag in the synopsis is rendered by the frontend from passages[].canon, not taken from the model's prose. Reason: measured on g19 in both-canon mode, the 8B cited a deuterocanonical passage and dropped the "(Deuterocanon)" marker the prompt told it to keep, so the label cannot rest on the model obeying an instruction any more than the verse text can. The prompt still asks for it; the guarantee no longer depends on the answer. P5 owns the rendering.
- 2026-08-26 — MEASUREMENT, no decision taken: the pinned release's Vulkan build runs the 8B at 87 to 93 tokens per second against 4.8 to 6.0 on the CPU build, with prompt processing at 253 against 62, turning a 177-second median answer into 12 seconds. Peak host RAM falls from 9,037 MB to 5,363 MB and peak VRAM is 7,504 MiB of 10,240 on an RTX 3080. Verifier verdicts and the zero-fabrication result are unchanged. Reason to record rather than act: PLAN gives bundling and runtime selection to P6, and nothing is known about machines with less VRAM or none.
- 2026-08-26 — llama.cpp is pinned at release b10639, commit 5e6a37cb115dc1074e274ac004373f5661909695. The win-cpu-x64, win-vulkan-x64 and ubuntu-x64 assets have their sha256 recorded in docs/SIDECAR.md, NOTICE.md and tools/fetch_llama.py, which refuses to unpack an archive whose checksum does not match. Reason: the model server is the process that reads the model and answers on the user's machine, and it is the one dependency that cannot be taken on trust.
- 2026-08-26 — The backend generates through /v1/chat/completions rather than P3's /completion. Reason: POST /apply-template proved the server builds byte-identically the prompt P3 built by hand, and both reported the same prompt token count, so this is a parity-preserving change; the think block is stripped in Rust anyway rather than relying on the server continuing to strip it.
- 2026-08-26 — The llama.cpp sidecar is placed in src-tauri/binaries by tools/fetch_llama.py --sidecar, with the server named llama-server-x86_64-pc-windows-msvc.exe per Tauri's externalBin convention, and the whole directory is gitignored bar its .gitkeep. Reason: the Windows build is not one file, so the 50 DLLs beside the server travel with it and P6 declares them as bundle resources; nothing binary is committed either way.

## P5 session, 2026-08-26

- 2026-08-26 — Hardware policy: one reference class of machine, stated in README as "Built and tested on: AMD Ryzen 7 5800X (8 cores), NVIDIA RTX 3080 10 GB, 32 GB RAM, Windows 11", with the measured numbers from EVAL.md and P4 behind it. Below it the app runs, slower, and README says so in one sentence with the measured CPU-only answer time. The app never refuses to run on hardware grounds. Reason: Jared's direction; one measured machine is the honest reference, and a floor invented from it would be a guess presented as a requirement. PLAN 6.5 amended.
- 2026-08-26 — The first-run hardware check is advisory: memory, free disk and graphics device are shown beside the reference machine, one plain warning line appears if anything is below it, and Continue is always enabled. Reason: follows from the hardware policy; replaces PLAN 7.1 step 2's refusal.
- 2026-08-26 — Qwen3-8B is the model, with no automatic selection. Qwen3-1.7B stays in Settings as "Smaller model: faster, needs less memory, gives list-style answers". Reason: every install giving the same answers matters more than fitting a machine we have not measured; the reader chooses the smaller model, and the caveat is stated where they choose it. PLAN 6.4 amended.
- 2026-08-26 — Compute is a setting, Auto / CPU / GPU. Auto uses the GPU sidecar when a Vulkan device is present and the CPU sidecar otherwise. P5 implements the setting with the CPU path only, GPU disabled and labelled "available in a later release"; P6 adds the Vulkan sidecar and the detection. Reason: P4 measured 12 seconds against 178 per answer, which is too large to leave undeclared, and the CPU path already exists.
- 2026-08-26 — Both sidecars, chat and embedding, stay loaded for as long as the app is open and are stopped on window close. No memory-threshold unloading. Reason: the reference machine has the memory, and swapping models in and out would make every answer slower for a case the reference machine does not have.
- 2026-08-26 — Generation can be cancelled. Cancel stops reading the streaming response, which closes the connection and makes llama-server abandon the slot; if the server is not idle within 2 seconds the chat sidecar is restarted. Reason: a two-and-a-half-minute wait on a CPU needs an exit, and a cancel that leaves the server busy would wedge the next question.
- 2026-08-26 — The passage panel groups by the root Nave's topic by default, found by walking parent_topic_id upwards, with the matched subtopic as a trimmed second line and a switch to group by book in canonical order. Reason: P4 flagged that Nave's subtopic headings are unusable as labels because some are whole paragraphs; the root heading is short and is what a reader recognises. Flagged for Jared's eyes.
- 2026-08-26 — A static modern-to-biblical vocabulary expansion table applied before search is the first post-v1 item, PLAN 15.1. Reason: P4 measured hand-written keyword lists at 0.4875 recall@25 against 0.3625 for the raw question, the largest retrieval gap left, and it is a data problem rather than a prompt problem.
- 2026-08-26 — data/disclaimer.txt is the single source for PLAN 9.2's wording, as data/crisis_note.txt already is for 9.3. The welcome screen and README both come from it and a test asserts they match. Reason: the two places a reader meets the disclaimer must not drift.
- 2026-08-26 — VERIFIED, PLAN section 16: the model host is huggingface.co, URL form https://huggingface.co/<org>/<repo>/resolve/main/<file>, no token needed, HTTP Range honoured, and all three files' sizes and sha256 match both the host and the copies on this machine. Recorded in docs/SIDECAR.md and NOTICE.md. Reason: the last VERIFY item P5 owns.
- 2026-08-26 — CORRECTION to PLAN 3.3: Tauri's capabilities gate what the frontend may do and cannot gate a Rust HTTP client, so the network policy is not enforced by a scoped capability as the plan says. It is enforced by an allow-list of the exact pinned model URLs in download.rs, which `fetch` checks by whole-string comparison and which a test exercises with three rejected URLs, plus loopback for the tests themselves. Reason: the plan named a mechanism that does not do what it was expected to do; the goal it named is met, and more tightly, but the wording was wrong and saying so is better than leaving a false claim in the plan.
- 2026-08-26 — TLS is rustls with the webpki-roots bundled root store rather than the platform's certificate store. Reason: a clean Windows machine needs nothing installed, and the one connection this program makes should not depend on the state of a system trust store. webpki-roots is MPL-2.0, used unmodified as a data file, and NOTICE.md records it.
- 2026-08-26 — A cancelled download keeps its partial file. Reason: cancelling should stop the wait, not throw away the four gigabytes the reader already waited for; the next attempt resumes from there.
- 2026-08-26 — A model file that is present but does not match its checksum is deleted and fetched again rather than used. Reason: the size alone would pass a truncated or substituted file, and a model file we cannot identify is the one thing that must never be run.
- 2026-08-26 — FINDING, P6 must know: `cargo build --release -p pastor-bible` produces a binary that loads devUrl (http://localhost:1420) rather than the embedded frontend, and shows "localhost refused to connect". The app must be built through the Tauri CLI, `npx tauri build`, which is what sets the build up to embed dist/. Reason: an hour was nearly lost to a blank window that looked like a rendering bug and was a build-command bug; P6's CI must call the CLI.
- 2026-08-26 — Screenshots are captured with PrintWindow(PW_RENDERFULLCONTENT), not by copying the screen. Reason: WebView2 draws into a composition surface that a screen copy misses entirely, which produced screenshots of the desktop behind the window with only the title bar of the app in them; tools/shot.ps1 falls back to a screen copy only if PrintWindow refuses.
- 2026-08-26 — The retrieved passages reach the window through a command, `retrieved_passages`, rather than in the `retrieved` event. Reason: the payload is about a quarter of a megabyte and does not survive the event channel; measured in the running app, the counts arrived and the list did not, with no error anywhere. The same data returns from a command without trouble, which is the channel the finished answer already uses.
- 2026-08-26 — Stop stops the answer within about two and a half seconds, by setting a flag and then, two seconds later, stopping the answering model outright if the answer is still running. Reason: a cancellation is only noticed between chunks of the response stream, and during prompt processing no chunk arrives for tens of seconds; measured at 16.3 seconds from Stop to control returning before this change and 2.69 seconds after it. The model is not reloaded on the way out: the next question pays the four seconds, so a reader who pressed Stop is not made to wait for something they may not want.
- 2026-08-26 — The passage panel groups by the ROOT Nave's topic, found by walking parent_topic_id upwards with a bounded loop, with the matched subtopic shown beneath as "matched under ...". Groups sharing a root are merged. Reason: retrieval matches leaves, and a leaf heading in Nave's can be a whole paragraph; the first build of this screen produced group titles like "INSTANCES OF Ahithophel Naaman, refusing to wash in the...", which is not a heading. The root is what a reader recognises.
- 2026-08-26 — MEASURED: a long-lived session, which is how the app runs, peaks at 11,763 MB across both sidecars over five questions, against P4's 9,001 MB for a fresh server per question and P3's 15,068 MB over ten. The app and its webview add 969 MB. Reason: P4's handoff said this had to be measured before README's memory figure could be trusted; README says about 9 GB while an answer is written, which is now understated for a long session and is corrected.
- 2026-08-26 — history.passage_ids stores one array of verse ids per sent passage, in the order they were sent, rather than a flat verse list. Reason: a flat list loses which verses were [P1] and which were [P2], and a reopened answer showed the reader a bare "[P19]" it could not resolve into a reference. Entries written in the flat form are still read, regrouped by adjacency, and say so in the code.
- 2026-08-26 — A reopened answer is grouped by book, not by topic. Reason: the topic grouping was never stored with the entry, and the first build showed a past answer under the topics of whatever had been asked most recently, which is worse than no grouping at all.

## P5.1 session, Jared's first-look fixes, 2026-08-27

- 2026-08-27 — CAUSE of the model-path defect, recorded before the fix. `src-tauri/src/lib.rs` resolved the model directory as `TPB_MODEL_DIR` if set and otherwise `%APPDATA%\io.github.haomuch1.pastorbible\models`, and built both model paths by joining a file name onto it. That directory has never existed on this machine and nothing in the program ever creates it: the chat model is downloaded there by a first-run screen that `first_run_done = 1` had already skipped, and the embedding model is not downloaded at all. Every P5 run launched the app the way docs/HANDOFF.md instructs, with `TPB_MODEL_DIR` pointing at the repository's `models\`, so the self-test passed; Jared ran `npm run tauri dev` with no such variable and the first model the pipeline loads, the embedding model, produced `The system cannot find the path specified (os error 3)` two and a half minutes into a question. Nothing was copied or moved during P5. Reason to record the cause and not only the fix: the defect was invisible to every test and every measurement because the environment variable was in the instructions rather than in the program.
- 2026-08-27 — The embedding model is resolved as a resource of the application, not as a file in the application data directory. `src-tauri/tauri.conf.json` declares `bundle.resources`, which `tauri-build` copies beside the binary in a development build and the installer ships in a release one, and the shell looks in the resource directory first, then at `src-tauri/resources/` for a checkout, and only then in the models directory. `TPB_EMBED_MODEL` overrides all of it. Reason: DECISIONS 2026-08-26 already said the embedding model ships in the installer; the code had it in app data, where nothing puts it. Jared's direction, and it also means a reader is never asked to place a file.
- 2026-08-27 — `tools/fetch_model.py` places the embedding model in `src-tauri/resources/`, pinning the same URL, byte count and sha256 that `src-tauri/core/src/download.rs` pins, refusing to leave a file whose checksum does not match, and copying from `models/` when the bytes are already on the machine. The file is gitignored; the script is source. Reason: the session brief said "the existing pinned-checksum script", and none existed for the models — `tools/fetch_llama.py` pins llama.cpp only. Flagged rather than improvised around: this is the smallest thing that makes the brief's sentence true.
- 2026-08-27 — Two tests stand behind it, `src-tauri/core/tests/bundled_model.rs`: the resolved path exists, is the pinned size, and its sha256 matches `download::MODELS`; and it resolves to the resource directory rather than to application data, which is the defect written down. The running app checks the same sha256 in a background thread at startup, off the path that opens the window in under a second.
- 2026-08-27 — A missing or mismatched model file produces a plain sentence naming the file, shown on the main screen at startup and returned by `ask` instead of running. Reason: the reader saw `os error 3` with a path in it, two and a half minutes after asking a question. A missing file is not an unexpected condition, and the message should arrive before the wait rather than after it. Screenshot 22.
- 2026-08-27 — The chat model stays in application data, where the downloader puts it, and a development build additionally accepts the repository's `models/` directory when application data has no copy. A release build never looks there. Reason: nobody building this app should have to copy five gigabytes or set a variable to run `tauri dev`, and that gap is exactly what produced the defect above; the reader's copy is still the reader's copy.
- 2026-08-27 — Every reference shown to a reader or written into an exported file uses the book name a reader writes: "1 Kings 3:9", never "1Ki 3:9". The table is `index::DISPLAY_NAMES`, keyed on USFM code, and it is the only book data in this program not read from index.db. Reason: index.db has two spellings and neither is it. `abbrev` is the Treasury of Scripture Knowledge's compact form and what the retrieval fixtures hold; `name` is the World English Bible's own running title, which for that book is "The First Book of Kings" and for one deuterocanonical book is "The Prayer of Manasses King of Judah when He was Held Captive in Babylon". The session brief named the `name` column and gave "1 Kings" as its example; the two do not agree, so the work stopped and Jared chose the ordinary names on 2026-08-27. A test asserts every book index.db holds has an entry.
- 2026-08-27 — The compact form survives in exactly one place: the prompt sent to the model. Reason: it is what P3 and P4 measured and what the committed retrieval fixtures pin, and changing the model's input would quietly invalidate every parity claim in this file. The reader never sees the prompt. `book_names.rs` asserts both halves so the split cannot close in either direction by accident.
- 2026-08-27 — The P3 fallback-rendering parity check compares the Rust output against the Python fixture with the book spellings mapped, rather than byte for byte. Reason: the fallback listing is shown to the reader and so moved to the reader's spelling; grouping, ordering, indentation and blank lines must still match Python exactly, which is what the test now asserts, and that is a stricter statement than comparing two strings that could only ever have differed there.
- 2026-08-27 — The history export resolves each `[P#]` marker into the reference it stood for and lists the passages underneath. Reason: "[P3]" means nothing on paper, and the export is the copy a reader keeps or prints.
- 2026-08-27 — user.db schema 2 deletes history entries whose passage numbering was never stored, and the nine such entries on this machine were deleted on 2026-08-27. Reason: those entries could only show their answer with the citation markers stripped out and a notice explaining why; no released version ever wrote that form, so the only entries in it are the test questions asked while P5 was being built. The notice outliving its cause is worse than losing test data. The delete goes through the table so the FTS index follows it.
- 2026-08-27 — A "New question" control sits at the top of the main area and in the sidebar header, and "Clear history" at the foot of the sidebar behind a confirmation, calling the same command as Settings. Reason: Jared found that opening a past answer had no visible way back, and that clearing history was reachable only through Settings.
- 2026-08-27 — Every passage in the panel can be opened in its chapter: the whole chapter from index.db, the cited verses marked and scrolled to, previous and next chapters, and the Deuterocanon tag carried from the index. It is an overlay, so closing it puts the reader back exactly where they were with nothing reloaded. Previous and Next step over the Deuterocanon in 66-book mode and into it in both-canon mode, but a chapter the reader asked for always opens whatever the setting, because a citation being followed must always resolve.

## P5.2 session, 2026-08-27

- 2026-08-27 — CORRECTION to a claim this project made twice and never checked. P5's report and P5.1's HANDOFF (line 70, "Per-entry delete is unchanged") both said the history sidebar had a per-entry delete. It never had one. `historyDelete` was exported from src/api.ts and called from nowhere; `grep -rn "historyDelete" src/` returned the definition and nothing else, and the sidebar entry rendered a single button holding the question and its date. Jared found it by hovering over every entry in the running app. What made the false claim easy is that everything beneath the button was real and tested: `UserDb::delete`, the `history_delete` command, the frontend binding, and a passing cargo test named `deleting_removes_exactly_one_and_search_forgets_it`. Reason to record it as a decision rather than a bug: a passing test on the command underneath is not evidence that a reader can reach it, and this project had no test that rendered a component at all until today.
- 2026-08-27 — The frontend gets a test runner: Vitest with jsdom and Testing Library, development-only, and `src/screens/Main.test.tsx` renders the sidebar. Reason: the failure above is invisible to every other kind of test this repository has. jsdom does no layout, so these tests cannot tell a visible button from a collapsed one; what they can prove is that the control exists, that it is reachable by the name it announces, and that pressing it deletes one entry, deletes no others, and does not open anything. The pixels were checked by clicking the control's coordinates in the running app.
- 2026-08-27 — Each history entry carries an always-visible delete control at its right edge, titled and labelled "Delete this question", with an inline Delete / Cancel confirm on that entry. Reason: a control that appears only on hover does not exist for a keyboard, a touch screen, or a reader who is not already expecting it, which is the same failure as having no control, one step further along. The entry is now a row containing an open button and a delete button rather than one button, so deleting cannot also navigate.
- 2026-08-27 — "Clear history" is removed from the sidebar. Delete all history stays in Settings, beside the export that would have saved the entries first. Reason: Jared's direction; deleting one entry and deleting every entry are different kinds of act and do not belong next to each other.
- 2026-08-27 — REVERSAL of the 2026-08-26 default: the passage panel groups by book, in canonical order, and the topic view is the secondary switch labelled "Group by topic". The reader's choice is remembered in settings. Reason: P4 found Nave's subtopics unusable as labels because some are whole paragraphs, and P5 moved to the roots, which was a real improvement; but the roots are not a category system either. An answer about giving to the poor grouped its passages under "HAMATH" and "TOB-ADONIJAH", roots that merely happen to contain a matching verse. A heading that is not about what the passages have in common is worse than no heading. Jared chose by book. PLAN 7.2 amended.
- 2026-08-27 — In book order a cited passage is marked where it falls rather than lifted to the top of its book, and expanding a book shows it from end to end in chapter and verse order. Reason: the whole point of canonical order is that it is canonical order; a reader who knows roughly where a passage sits can find it, and reordering inside the group would take that back. Topic order has no such order to keep and stays cited-first.
- 2026-08-27 — Settings offers a choice of file before the save dialog: a text file, unchanged, or a spreadsheet. No CSV. Reason: Jared's direction. The text file is the copy someone prints; the workbook is the copy someone sorts, filters and hands on, and a CSV is neither, because one row per passage across many questions is not one table.
- 2026-08-27 — The spreadsheet writer is rust_xlsxwriter 0.99.0, MIT or Apache-2.0, pure Rust with no native dependency, bringing zip (MIT), typed-path (MIT or Apache-2.0), zopfli (Apache-2.0) and zlib-rs (Zlib), all of them also pure Rust. Recorded in NOTICE.md. calamine 0.36.1 (MIT) reads xlsx and is a development dependency only: the tests write with one library and read back with another, so a passing test cannot mean the two agreed on a format neither got right.
- 2026-08-27 — A question sheet whose entry was written against a different Bible index says so in one line and lists its references without verse text. Reason: elsewhere in this app a re-rendered passage carries a note beside it, and the reader can see the note and the text together. A spreadsheet is exactly the artefact somebody opens later, sorts, and trusts without reading the caveat, so the text is withheld rather than captioned.
- 2026-08-27 — The plain-text export and the spreadsheet are both written from one reader, `UserDb::entries`, rather than each reading the history table for itself. Reason: two exports of the same entry that disagree is a defect nobody would find until someone compared two files.
- 2026-08-27 — Theme headings are written into a spreadsheet cell without their markdown hashes and set in bold instead. Reason: found by opening the file the running app produced; "## Tithing as a Divine Commandment" is what markdown looks like when nothing renders it, and a cell renders nothing.

## P6 session, packaging and CI, 2026-08-27

- 2026-08-27 — MEASURED, and it reshapes the sidecar decision: there is one llama.cpp build, not two. Every file in the win-vulkan-x64 archive is byte-identical to the file of the same name in win-cpu-x64, and the Vulkan archive holds exactly one file the CPU archive does not, `ggml-vulkan.dll`; on Linux the same holds with `libggml-vulkan.so`. Copying that one file beside the CPU build turns `--list-devices` from "(none)" into "Vulkan0: NVIDIA GeForce RTX 3080 (10267 MiB, 9495 MiB free)" from the same binary, because ggml loads its backends as dynamic libraries at run time. Reason to record rather than follow the brief's wording: "both sidecars bundled" would have shipped 99 MB of duplicate files, and the goal it named — the GPU path available, the CPU path as fallback, both in the installer — is met by one server and one extra library.
- 2026-08-27 — CORRECTION to PLAN 11's "sidecar binaries per target triple per Tauri's externalBin convention". externalBin resolves one file per name per triple and places it beside the program; it cannot express a server that loads twenty-two libraries from its own directory. The sidecar is a bundle resource directory, `resources/llama/`, and the app resolves the server inside it. Reason: Windows searches the executable's own folder for a DLL first, so the server and its libraries cannot be separated; the plan named a mechanism that does not do what it was expected to do, and saying so is better than leaving a false claim in it.
- 2026-08-27 — The bundled sidecar is trimmed to what llama-server loads: 23 files, 90.3 MB, against the release archive's 51. Established by removing files until it stopped starting — dropping mtmd makes it exit 0xC0000135, DLL not found, so mtmd stays. Reason: PLAN 7.5 says the install directory holds the program, the sidecar and the index and nothing else, and llama-cli, llama-bench, llama-perplexity and llama-quantize are nothing else.
- 2026-08-27 — GPU detection is `llama-server --list-devices`, parsed. It loads no model, takes about a second, and needs no GPU-specific code in this program. The decision is made once per session and again when the compute setting or the model changes. `TPB_NO_GPU` forces it to find nothing, for tests. Reason: the alternative was linking a Vulkan enumeration library into the app to answer a question the sidecar already answers.
- 2026-08-27 — Auto uses the graphics card when its **free** memory covers the model, not its total: measured at full offload with the 8192-token context, 5,750 MiB for Qwen3-8B-Q4_K_M and 2,722 MiB for Qwen3-1.7B-Q8_0, pinned as those plus a tenth — 6,325 and 2,994 MiB. Measured as the rise in whole-GPU used memory from just before llama-server started to its peak while answering a 1,097-token prompt, because per-process graphics memory is not reportable under Windows' display driver model. Reason: a card with ten gigabytes and nine already spoken for cannot hold the model, and starting anyway would fail slowly instead of quickly.
- 2026-08-27 — The search model always runs on the processor, never the card. Reason: it takes about a second there, and every megabyte of the card is worth more to the model that would otherwise take minutes.
- 2026-08-27 — An explicit "Graphics card" setting is honoured even when the card looks too small, with the shortfall stated in the message. Reason: the free-memory figure is a report from a driver, not a promise, and a reader who knows their machine should be able to overrule it.
- 2026-08-27 — MEASURED from the shipped binary: the ten P3 graded questions, asked through the installed window, all ten verified with no fallback, median 6.5 seconds and 13.2 seconds for the first of a session including the model load. With the probe forced to fail the same question was answered on the processor in 134.0 seconds. README's GPU claim is now this measurement rather than P4's harness figure.
- 2026-08-27 — index.db ships with only the embedding model the app searches with: 955.6 MB to 366.8 MB, which is the figure P3 recorded. `pipeline/strip_index.py` does it at build time and keeps the full index at `data/index-full.db` for a later re-evaluation. All 70 cargo tests, the 14 retrieval-parity cases included, pass against the stripped file, so it changed no result.
- 2026-08-27 — index.db and the embedding model are declared bundle resources, so `tauri build` refuses to produce an installer without them. Reason: P5.1 left index.db resolving through a fallback chain, which meant a build could quietly succeed with no Bible in it.
- 2026-08-27 — The version lives in `src-tauri/Cargo.toml` and the `version` key is removed from `tauri.conf.json`, where Tauri falls back to the Cargo one. Reason: they disagreed. About read `CARGO_PKG_VERSION` and said 0.0.1 while the installer read tauri.conf.json and said 0.9.1, and the upgrade test is the thing that found it.
- 2026-08-27 — CONFIRMED, P0's default kept: Windows installs are NSIS, per user, into `%LOCALAPPDATA%\The Pastor Bible`. Reason: it needs no administrator password, which matters for a free tool a stranger is being asked to trust; it puts a per-user app's data and program in the same place; and on a shared machine each account installs its own copy and keeps its own questions, which is the honest arrangement for an app whose whole point is that nothing leaves the reader's hands. MSI was not taken: it is built for administrators deploying to fleets, and would need elevation for a program nobody else on the machine can use.
- 2026-08-27 — MEASURED and then fixed: Tauri's NSIS template does not refuse a downgrade. It compares versions only to word the reinstall page, and `/S` shows no page, so the 0.9.0 installer replaced a 0.9.1 installation silently. `src-tauri/installer.nsh` adds `NSIS_HOOK_PREINSTALL`, which compares and aborts with a plain message and exit code 4 before anything is written. Verified: exit 4, Add/Remove still 0.9.1, 27 files, history and model untouched.
- 2026-08-27 — The uninstaller asks whether to delete the reader's questions and their downloaded model, defaulting to keep, in `NSIS_HOOK_PREUNINSTALL`. The wording names the folder and says that keeping means not downloading five gigabytes again. The web view's own cache is not the reader's data and is always removed. Verified both ways: No leaves the app data and removes the install directory; Yes removes the app data and the model.
- 2026-08-27 — WebView2 is installed by the `downloadBootstrapper`, silently. Reason: it is about 2 MB, it is the option that needs nothing of a reader on a clean Windows 10 machine, and the alternatives either embed 130 MB in every installer or assume a runtime that may not be there. The clean-machine test is P7's.
- 2026-08-27 — index.db is published as an asset of the `index-0.2.0` prerelease in this repository, pinned by sha256, and CI fetches it from there. Reason: a build needs it beside the binary or `tauri build` refuses, a runner starts empty, and 366.8 MB is past GitHub's per-file limit for git. `.gitignore` already said "shipped as a release asset"; this is that. Jared approved the upload on 2026-08-27.
- 2026-08-27 — CI runs on a `v*` tag or by hand, never on push, and the workflow carries a `dry_run` input that checks the plumbing without bundling. Reason: the repository is private until v1.0.0 and Actions minutes on a private repository are metered; the three cheap dry runs that found the real faults cost 17 minutes against the full run's 65.
- 2026-08-27 — The offline gate runs the query suite inside a network namespace holding only loopback, and then proves the namespace by asserting that reaching the model host from inside it fails **and** that loopback still works. Reason: a gate that quietly had a network would be worse than no gate, and so would one that passed because it had no server to talk to either.
- 2026-08-27 — The offline gate covers the query suites and not the two model-lifecycle suites, which drive a real llama-server with the 4.7 GB chat model and fail loudly rather than skip when it is absent. Reason: that behaviour is right on a developer's machine and wrong on a runner that should never fetch five gigabytes; the workflow names the suites it runs rather than appearing to run them all.
- 2026-08-27 — Only a plain `vX.Y.Z` counts as a released version for the increase gate. Reason: `index-*` is a build input and a suffixed tag like `v0.9.1-ci` is a test of the workflow; without this the gate compared a tagged build against its own tag and refused every release.
- 2026-08-27 — VERIFIED by a full tagged run: version, offline, Windows NSIS, Linux .deb and .AppImage, upgrade gate and draft publish all passed. The upgrade gate found no previous release and said so in the log while running the candidate twice, which tests reinstall rather than a version change; the first real release makes it a version change by itself.
- 2026-08-27 — NOT VERIFIED and left to P7: the SmartScreen warning README 9.4 describes. The installer was given a browser's mark-of-the-web and run, and no warning appeared on this machine, which has run these bytes many times and has a local reputation for them. A machine that has never seen the file is the only place that claim can be checked, and that is what P7 is.

## P7-prep session, the v1.0.0 candidate, 2026-08-27

- 2026-08-27 — MEASURED and then fixed: the installer shipped llama.cpp's binaries with no licence text of any kind. NOTICE.md said "The MIT licence text ships beside the binaries in the archive and is included in the installer", and neither half was true. The release archives carry no llama.cpp LICENSE at all — the only licence file in them is `LICENSE-LLVM-OpenMP`, which covers libomp — and `bundle()` selected files by extension, so nothing but the server and its DLLs was ever copied. Both texts are now vendored in `src-tauri/licenses/`, `tools/fetch_llama.py --bundle` copies them into `resources/llama/` and raises rather than assembling without them, and a cargo test asserts they are in the repository and are the licences they claim to be. Reason: MIT and Apache-2.0-with-LLVM-exceptions both require the notice to travel with any copy of the software, an installer is such a copy, and this was the one finding in the pre-release check with a real obligation behind it rather than a wording error.
- 2026-08-27 — The bundled sidecar is 25 files, not the 23 P6 recorded: 23 binaries plus the two licence texts, still 90.3 MB, because the texts are 20.8 KB between them. Amends the P6 entry that pinned it at 23. The installed directory is 29 files rather than 27.
- 2026-08-27 — Credits live in `pastor_bible_core::credits` and nowhere else: the authors, this project's licence, and the seven sources with theirs. The About screen builds its list from it, and `tests/credits.rs` asserts that README's "Sources and credits" section lists exactly the same pairs in the same order, that it carries the About screen's "Made by" line, and that both names appear in README's Authors section. The test runs in CI's offline gate. Reason: the same move the version made in P6, for the same reason. The pre-release check asked whether About matched the README credits and found there was nothing to match — About had carried the full list since P5 and the README section was still "Not yet available; filled in at P3." A fact a reader can see in two places will eventually disagree with itself unless a test stands between them.
- 2026-08-27 — README's three placeholder sections are written from the app as built: "Using it" (the question box, the canon toggle and what turning it on actually does, the citation chips and the passage panel, Read chapter, the model and compute choices, the history and deleting from it, both export formats, and what happens when a summary cannot be verified), "Building from source" (toolchain, the three checksummed fetches, the build and test commands, and the macOS note the dropped-macOS decision depends on), and "Sources and credits". Reason: PLAN 9.1 requires all three, every phase they deferred to is finished, and P7's clean-machine script tells Jared to follow only the README — a README that does not describe using the app would have made every step of that test read as a defect.
- 2026-08-27 — CORRECTED in NOTICE.md: the "Model files" section said "None. Models are chosen in P3 ... are not distributed by this project." It was written before P3 chose them and left standing after. It contradicted the same file's own "Pinned binaries and models" section, and it was wrong about the search model, which is bundled in the installer and so is distributed by this project. The section now names all three and states which of the two ways each reaches a machine, because that difference is what decides whose attribution obligation it is.
- 2026-08-27 — CORRECTED in NOTICE.md: the transitive-dependency section promised a licence audit and the bundling of third-party licence texts "is done in P6", in the future tense, after P6 had finished — and the bundling it promised had not happened. It now states what actually ships and what is merely compiled in.
- 2026-08-27 — CORRECTED in the release notes the workflow writes: they said "The README's install section has the screenshots." It does not, and `docs/screenshots/` has no install images, because P6 could not produce them on a machine with a local reputation for the file. The notes now say the screenshots are added after the clean-machine test, and say why. Reason: a draft release built for the clean-machine test must not carry a false claim about the very thing that test exists to produce.
- 2026-08-27 — CORRECTED in README: the disk figure read 5.4 GB in one place and 5.3 GB in another, eleven lines apart. The measured value is 5.4 — 734.8 MiB installed plus the 4,795.0 MiB answering model is 5,529.8 MiB — so the 5.3 was the error.
- 2026-08-27 — CORRECTED in README, not among the findings first reported: the graphics-memory figure read "about 5.4 GB", which is the disk figure copied by mistake. The measurement in this file is 5,750 MiB at full offload, which is 5.6 GB. Reason: the instruction was to reconcile a figure to its measured value, and shipping a second wrong number beside the one being fixed would have been indefensible.
- 2026-08-27 — README's install section no longer says "There is no release yet: v1.0.0 is the first." Reason: the sentence stops being true the moment the release it describes exists, and the README is read from the release page.
- 2026-08-27 — MEASURED by the first real tag, and then fixed: the version gate refused v1.0.0 with "version 1.0.0 is already released", comparing the candidate against its own tag. It read `git tag -l 'v*'`, and the tag being built is in the checkout. P6's entry above claims this fault was fixed and that a full tagged run verified it; both are now qualified. P6's fix filtered the tag list to plain `vX.Y.Z`, which excluded `v0.9.1-ci` — the only tag any P6 run could have collided with — so the run that "verified" the gate never exercised the path a release takes. A gate tested only by a tag shaped unlike a release was not tested. The gate now asks `gh release list --exclude-drafts` what has actually been published, which is what the step's own name always claimed, and which the upgrade gate has asked all along. Drafts are excluded so that P7's fail path — delete the draft, delete the tag, retag — can run again.
